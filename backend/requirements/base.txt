# ── BASE REQUIREMENTS (shared by dev and prod) ────────────────

# Django core
Django==4.2.13

# REST API framework — turns Django into a powerful API server
djangorestframework==3.15.1

# JWT authentication (JSON Web Tokens)
# Short-lived access tokens + refresh token rotation
djangorestframework-simplejwt==5.3.1

# Postgres database adapter for Python
psycopg2-binary==2.9.9

# Redis cache backend for Django
django-redis==5.4.0

# CORS headers — allows our React frontend to call the Django API
django-cors-headers==4.3.1

# Environment variable management — reads from .env file
python-decouple==3.8

# Image processing (profile pictures, etc.)
Pillow==10.3.0

# Auto-generate OpenAPI docs + Swagger UI from your DRF code
drf-spectacular==0.27.2

# Rate limiting for API endpoints (e.g. max 5 login attempts/min)
django-ratelimit==4.1.0

# Better filtering for DRF list endpoints (?specialization=cardiology)
django-filter==23.5
